#/usr/bin/env bash

export DYLD_LIBRARY_PATH=/Users/frangio/Applications/SCIP-8.0.0/lib

# Mnetgen - - - - - - - - - - - - - - - - - - - -

for i in 64
do
for j in 4 8 16 32 64
do
for (( h=1 ; h<13 ; h++ ))
do

printf "[data/Mnetgen/$i-$j-$h m]: "

./LDS_MMCF_test data/Mnetgen/$i-$j-$h m

#retVal=$?
#if [ $retVal -ne 0 ]; then
#    exit 1
#fi

done
done
done

for i in 128
do
for j in 4 8 16 32 64 128
do
for (( h=1 ; h<13 ; h++ ))
do

printf "[data/Mnetgen/$i-$j-$h m]: "

./LDS_MMCF_test data/Mnetgen/$i-$j-$h m

#retVal=$?
#if [ $retVal -ne 0 ]; then
#    exit 1
#fi

done
done
done

#for i in 256
#do
#for j in 4 8 16 32 64 128 256
#do
#for (( h=1 ; h<13 ; h++ ))
#do

#printf "[data/Mnetgen/$i-$j-$h m]: "

#./LDS_MMCF_test data/Mnetgen/$i-$j-$h m

#retVal=$?
#if [ $retVal -ne 0 ]; then
#    exit 1
#fi

#done
#done
#done

# end batch
